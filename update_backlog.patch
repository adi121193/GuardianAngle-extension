--- PRODUCT_BACKLOG.md
+++ PRODUCT_BACKLOG.md
@@ -52,19 +52,19 @@
 * **User Story:** As an enterprise IT admin, I want to deploy this extension to my 500 employees and know it will reliably intercept PII regardless of what UI experiments or A/B tests OpenAI is running this week.
 * **Developers Required:** 1 Extension Architecture Expert

-### 4. Zero-Knowledge Federated Learning for False Positives
-**What will happen:** Instead of manually maintaining Regex rules, the extension will use Local Differential Privacy. When a user explicitly chooses to "Send Anyway" (indicating a false positive), the extension locally hashes the contextual metadata and sends an anonymized, mathematically un-reversible weight update to a central aggregator.
-* **Why it's 10x Better:** The community trains the engine. If 1,000 developers click "Send Anyway" on AWS ARNs (which look like PII), the extension globally learns to ignore that pattern context without a single byte of actual user data ever leaving their machine.
-* **User Story:** As a user, I want the extension to become smarter over time and stop alerting me on fake test data, without my personal data ever being transmitted to your servers.
-* **Developers Required:** 1 ML Engineer, 1 Backend Data Engineer
+### 4. On-Device Personalization Engine (Zero Server)
+**What will happen:** Instead of sending *any* data to a centralized server for federated learning, we implement a 100% local, on-device personalization loop. When a user frequently clicks "Send Anyway" on specific patterns (e.g., AWS ARNs or internal company ID formats that look like Social Security Numbers), the extension's local heuristic engine trains itself *only on that user's browser* using IndexedDB.
+* **Why it's 10x Better:** It maintains the absolute zero-server, privacy-first guarantee. The extension learns the user's specific workflow and false-positive tendencies completely offline. There is zero risk of data leakage because there is literally no backend API to send data to.
+* **User Story:** As a user, I want the extension to adapt to my unique workflow and stop alerting me on my company's specific non-PII ID formats, without any data ever leaving my local browser instance.
+* **Developers Required:** 1 Frontend/Storage Engineer
